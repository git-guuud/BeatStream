// Placeholder — Artist profile page
// Route: /beatstream/artist/[id]
export default function ArtistPage({ params }: { params: { id: string } }) {
  return <div>Artist profile: {params.id} (TODO)</div>;
}
